version: 2

models:
  - name: core_orders
    description: "Validated and deduplicated Silver Core orders."

    columns:
      - name: event_id
        data_tests:
          - not_null

      - name: dq_overall
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: [0, 1]
